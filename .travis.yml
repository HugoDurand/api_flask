language: python

services:
  - docker

env:
  - DOCKER_COMPOSE_VERSION=1.4.2

before_install:
  - docker-compose build
  - docker-compose up
  - docker-compose exec web pip install pep8

script:
  - find . -name \*.py -exec pep8 --ignore=E402 {} +